import { BarChart, ResponsiveContainer } from "recharts";
import styled from "styled-components";

export const ChatContainer = styled(ResponsiveContainer)`
  background-color: blue;
`;

export const Chart = styled(BarChart)`
  width: 100%;
  height: 100%;
  padding: 0;
  margin: 0;
`;
